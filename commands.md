# setting path for evan hall computer to use nodejs
$env:PATH = "C:\Users\ipipi2022\Desktop\node-v24.13.0-win-x64;$env:PATH"

# git commands

git push --force:
1. push by force
2. Lets say we have commit message that we already pushed to github
3. If we want to change it
4. we can retreive the old commit message
5. change the content
6. push by force because it has to overide the remote repo commit message

